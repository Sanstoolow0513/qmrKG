# Copy to .env and fill in values. Optional keys override config.yaml for the OCR (VLM) task.

PPIO_API_KEY=your_api_key_here

# Optional overrides (see src/qmrkg/llm_config.py)
PPIO_BASE_URL=
PPIO_VLM_MODEL=
PPIO_IMAGE_DETAIL=
PPIO_RPM=
